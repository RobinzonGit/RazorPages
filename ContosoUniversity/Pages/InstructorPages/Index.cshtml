@page
@model ContosoUniversity.Pages.InstructorPages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Instructor[0].LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Instructor[0].FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Instructor[0].HireDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Instructor[0].FullName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Instructor[0].OfficeAssignment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Instructor[0].Courses)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Instructor) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(model => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(model => item.HireDate)
            </td>
            <td>
                @Html.DisplayFor(model => item.FullName)
            </td>
            <td>
                @Html.DisplayFor(model => item.OfficeAssignment)
            </td>
            <td>
                @Html.DisplayFor(model => item.Courses)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
